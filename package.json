{
  "name": "pixy",
  "version": "0.4.3",
  "private": true,
  "description": "AI-powered Discord ticket assistant with guild-scoped knowledge and escalation routing.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate deploy",
    "prisma:migrate:dev": "prisma migrate dev",
    "pretest": "prisma generate",
    "test": "node --test",
    "pretest:guild-isolation": "prisma generate",
    "test:guild-isolation": "node --test test/guild-isolation.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@prisma/adapter-better-sqlite3": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "better-sqlite3": "^12.11.1",
    "discord.js": "^14.26.4",
    "dotenv": "^17.4.2",
    "groq-sdk": "^1.3.0"
  },
  "devDependencies": {
    "prisma": "^7.8.0"
  },
  "overrides": {
    "undici": "6.27.0",
    "@hono/node-server": "1.19.13"
  }
}
